{
  "buildCommand": "cd apps/web && npm install && npm run build",
  "outputDirectory": "apps/web/.next",
  "installCommand": "npm install --prefix packages/database && npm install --prefix packages/shared-types && npm install --prefix apps/web",
  "framework": "nextjs"
}
